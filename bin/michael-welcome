#!/usr/bin/env bash

set -u

STATE_DIR="${XDG_STATE_HOME:-$HOME/.local/state}/michael"
LOG_FILE="$STATE_DIR/welcome.log"

mkdir -p "$STATE_DIR"

{
    echo "[$(date '+%Y-%m-%d %H:%M:%S')] michael-welcome desativado: sem áudio de inicialização."
} >> "$LOG_FILE" 2>&1

exit 0
